// TODO: change later to a smaller number
// TODO: when we will implement a proper pagination
/**
 * The maximum number of items to be displayed per page.
 *
 * This constant is used for pagination and defines the maximum number of items
 * returned in a single request or page. It ensures that data is retrieved in manageable chunks.
 *
 * @constant {number} PAGE_SIZE - The maximum number of items per page (default: 1000).
 */
export const PAGE_SIZE = 1000
